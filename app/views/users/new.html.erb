<h1>New User</h1>

<%= form_for(@user) do |f| %>
  <%= render 'shared/error_messages', {object: @user} %>

  <div>
    <%= f.label :login %>
    <%= f.text_field :login %>
  </div>

  <div>
    <%= f.submit "Create", class: "btn btn-large btn-primary" %>
  </div>
<% end %>