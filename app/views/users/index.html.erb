<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.login, user %>
      (<%= link_to "delete", user, method: :delete,
        data: { confirm: "You sure 100% ?" } %>)
    </li>
  <% end %>
</ul>